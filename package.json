{
  "name": "cofri",
  "version": "0.0.1",
  "private": true,
  "description": "Assistente pessoal de finanças e tarefas via Telegram, com dashboard web.",
  "packageManager": "pnpm@11.1.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "bot:dev": "pnpm --filter @cofri/bot dev",
    "bot:build": "pnpm --filter @cofri/bot build",
    "bot:start": "pnpm --filter @cofri/bot start",
    "web:dev": "pnpm --filter @cofri/web dev",
    "db:generate": "pnpm --filter @cofri/db generate",
    "db:migrate": "pnpm --filter @cofri/db migrate",
    "db:studio": "pnpm --filter @cofri/db studio",
    "typecheck": "pnpm -r --parallel typecheck",
    "test": "pnpm -r --parallel test"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
