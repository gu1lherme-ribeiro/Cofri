# Postgres (Neon)
DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"

# Telegram bot
TELEGRAM_BOT_TOKEN=""

# JWT secret for magic-link auth (gerar com: openssl rand -base64 32)
AUTH_JWT_SECRET=""

# AES-256-GCM master key, base64, 32 bytes (gerar com: openssl rand -base64 32)
# Usado para envelopar as API keys BYOK dos usuários.
ENCRYPTION_MASTER_KEY=""

# URL pública do dashboard (usada no link mágico)
DASHBOARD_URL="http://localhost:3000"

# Fallback opcional (dev only) — em produção a chave vem da tabela `ApiKey`
# (BYOK do usuário, AES-256-GCM com ENCRYPTION_MASTER_KEY).
# Se vazia, o bot exige BYOK e responde com instrução pra /dashboard.
# Aceita sk-ant-… (Anthropic) ou sk-…/org-… (OpenAI). Provider é detectado.
LLM_API_KEY=""

# Bot delivery mode:
#   - "polling" para dev local
#   - "webhook" para EasyPanel/produção
BOT_MODE="polling"

# Webhook (apenas quando BOT_MODE=webhook)
BOT_WEBHOOK_URL=""
BOT_WEBHOOK_SECRET=""
PORT="8080"

# URL pública do WebSocket de tempo real (dashboard ↔ bot).
# O bot serve WSS na rota /ws no mesmo HTTP server do webhook.
# Em produção, aponta pro domínio público do bot atrás do reverse proxy do EasyPanel.
# Prefixo NEXT_PUBLIC_ é necessário pra ficar disponível no bundle do Next.
# Deixe vazio em dev (web:dev) se não quiser conectar no bot local.
NEXT_PUBLIC_REALTIME_WS_URL="wss://cofri-bot.duckdns.org/ws"
